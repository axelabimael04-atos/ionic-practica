import { FormatPricingPipe } from './format-pricing.pipe';

describe('FormatPricingPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPricingPipe();
    expect(pipe).toBeTruthy();
  });
});
